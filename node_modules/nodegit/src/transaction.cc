// This is a generated file, modify: generate/templates/class_content.cc.
#include <nan.h>
#include <string.h>
#include <chrono>
#include <thread>

extern "C" {
  #include <git2.h>
 }

#include "../include/functions/copy.h"
#include "../include/macros.h"
#include "../include/transaction.h"

 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitTransaction::GitTransaction(git_transaction *raw, bool selfFreeing) {
    this->raw = raw;
    this->selfFreeing = selfFreeing;
  }

  GitTransaction::~GitTransaction() {
 
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
 
  }

  void GitTransaction::InitializeComponent(Handle<v8::Object> target) {
    NanScope();

    Local<FunctionTemplate> tpl = NanNew<FunctionTemplate>(JSNewFunction);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(NanNew<String>("Transaction"));

  
    Local<Function> _constructor_template = tpl->GetFunction();
    NanAssignPersistent(constructor_template, _constructor_template);
    target->Set(NanNew<String>("Transaction"), _constructor_template);
  }

  NAN_METHOD(GitTransaction::JSNewFunction) {
    NanScope();

    if (args.Length() == 0 || !args[0]->IsExternal()) {
         return NanThrowError("A new GitTransaction cannot be instantiated.");
     }

    GitTransaction* object = new GitTransaction(static_cast<git_transaction *>(Handle<External>::Cast(args[0])->Value()), args[1]->BooleanValue());
    object->Wrap(args.This());

    NanReturnValue(args.This());
  }

  Handle<v8::Value> GitTransaction::New(void *raw, bool selfFreeing) {
    NanEscapableScope();
    Handle<v8::Value> argv[2] = { NanNew<External>((void *)raw), NanNew<Boolean>(selfFreeing) };
    return NanEscapeScope(NanNew<Function>(GitTransaction::constructor_template)->NewInstance(2, argv));
  }

  git_transaction *GitTransaction::GetValue() {
    return this->raw;
  }

  git_transaction **GitTransaction::GetRefValue() {
    return this->raw == NULL ? NULL : &this->raw;
  }

  void GitTransaction::ClearValue() {
    this->raw = NULL;
  }

     Persistent<Function> GitTransaction::constructor_template;
 