// This is a generated file, modify: generate/templates/struct_content.cc.
#include <nan.h>
#include <string.h>
#include <chrono>
#include <thread>

extern "C" {
  #include <git2.h>
 }

#include <iostream>
#include "../include/functions/copy.h"
#include "../include/rebase_options.h"

 
using namespace v8;
using namespace node;
using namespace std;


// generated from struct_content.cc
GitRebaseOptions::GitRebaseOptions() {
   git_rebase_options wrappedValue = GIT_REBASE_OPTIONS_INIT;
   this->raw = (git_rebase_options*) malloc(sizeof(git_rebase_options));
  memcpy(this->raw, &wrappedValue, sizeof(git_rebase_options));

  this->ConstructFields();
  this->selfFreeing = true;
}

GitRebaseOptions::GitRebaseOptions(git_rebase_options* raw, bool selfFreeing) {
  this->raw = raw;
  this->ConstructFields();
  this->selfFreeing = selfFreeing;
}

GitRebaseOptions::~GitRebaseOptions() {
          
  if (this->selfFreeing) {
    free(this->raw);
  }
}

void GitRebaseOptions::ConstructFields() {
          }

void GitRebaseOptions::InitializeComponent(Handle<v8::Object> target) {
  NanScope();

  Local<FunctionTemplate> tpl = NanNew<FunctionTemplate>(JSNewFunction);

  tpl->InstanceTemplate()->SetInternalFieldCount(1);
  tpl->SetClassName(NanNew<String>("RebaseOptions"));

      tpl->InstanceTemplate()->SetAccessor(NanNew<String>("version"), GetVersion, SetVersion);
        tpl->InstanceTemplate()->SetAccessor(NanNew<String>("quiet"), GetQuiet, SetQuiet);
        tpl->InstanceTemplate()->SetAccessor(NanNew<String>("rewriteNotesRef"), GetRewriteNotesRef, SetRewriteNotesRef);
   
  Local<Function> _constructor_template = tpl->GetFunction();
  NanAssignPersistent(constructor_template, _constructor_template);
  target->Set(NanNew<String>("RebaseOptions"), _constructor_template);
}

NAN_METHOD(GitRebaseOptions::JSNewFunction) {
  NanScope();
  GitRebaseOptions* instance;

  if (args.Length() == 0 || !args[0]->IsExternal()) {
    instance = new GitRebaseOptions();
  }
  else {
    instance = new GitRebaseOptions(static_cast<git_rebase_options*>(Handle<External>::Cast(args[0])->Value()), args[1]->BooleanValue());
  }

  instance->Wrap(args.This());

  NanReturnValue(args.This());
}

Handle<v8::Value> GitRebaseOptions::New(void* raw, bool selfFreeing) {
  NanEscapableScope();

  Handle<v8::Value> argv[2] = { NanNew<External>((void *)raw), NanNew<Boolean>(selfFreeing) };
  return NanEscapeScope(NanNew<Function>(GitRebaseOptions::constructor_template)->NewInstance(2, argv));
}

git_rebase_options *GitRebaseOptions::GetValue() {
  return this->raw;
}

git_rebase_options **GitRebaseOptions::GetRefValue() {
  return this->raw == NULL ? NULL : &this->raw;
}

void GitRebaseOptions::ClearValue() {
  this->raw = NULL;
}

    NAN_GETTER(GitRebaseOptions::GetVersion) {
      NanScope();

      GitRebaseOptions *wrapper = ObjectWrap::Unwrap<GitRebaseOptions>(args.This());

        NanReturnValue(NanNew<Number>(wrapper->GetValue()->version));
     }

    NAN_SETTER(GitRebaseOptions::SetVersion) {
      NanScope();

      GitRebaseOptions *wrapper = ObjectWrap::Unwrap<GitRebaseOptions>(args.This());

         if (value->IsNumber()) {
          wrapper->GetValue()->version = (unsigned int) value->Int32Value();
        }
     }

      NAN_GETTER(GitRebaseOptions::GetQuiet) {
      NanScope();

      GitRebaseOptions *wrapper = ObjectWrap::Unwrap<GitRebaseOptions>(args.This());

        NanReturnValue(NanNew<Number>(wrapper->GetValue()->quiet));
     }

    NAN_SETTER(GitRebaseOptions::SetQuiet) {
      NanScope();

      GitRebaseOptions *wrapper = ObjectWrap::Unwrap<GitRebaseOptions>(args.This());

         if (value->IsNumber()) {
          wrapper->GetValue()->quiet = (int) value->Int32Value();
        }
     }

      NAN_GETTER(GitRebaseOptions::GetRewriteNotesRef) {
      NanScope();

      GitRebaseOptions *wrapper = ObjectWrap::Unwrap<GitRebaseOptions>(args.This());

        if (wrapper->GetValue()->rewrite_notes_ref) {
          NanReturnValue(NanNew<String>(wrapper->GetValue()->rewrite_notes_ref));
        }
        else {
          NanReturnUndefined();
        }

     }

    NAN_SETTER(GitRebaseOptions::SetRewriteNotesRef) {
      NanScope();

      GitRebaseOptions *wrapper = ObjectWrap::Unwrap<GitRebaseOptions>(args.This());

        if (wrapper->GetValue()->rewrite_notes_ref) {
        }

        String::Utf8Value str(value);
        wrapper->GetValue()->rewrite_notes_ref = strdup(*str);

     }

   
Persistent<Function> GitRebaseOptions::constructor_template;
