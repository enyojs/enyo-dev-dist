// This is a generated file, modify: generate/templates/class_content.cc.
#include <nan.h>
#include <string.h>
#include <chrono>
#include <thread>

extern "C" {
  #include <git2.h>
 }

#include "../include/functions/copy.h"
#include "../include/macros.h"
#include "../include/status_list.h"

  #include "../include/status_list.h"
  #include "../include/diff_perfdata.h"
  #include "../include/repository.h"
  #include "../include/status_options.h"
 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitStatusList::GitStatusList(git_status_list *raw, bool selfFreeing) {
    this->raw = raw;
    this->selfFreeing = selfFreeing;
  }

  GitStatusList::~GitStatusList() {
      if (this->selfFreeing) {
        git_status_list_free(this->raw);
        this->raw = NULL;
      }
 
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
                
  }

  void GitStatusList::InitializeComponent(Handle<v8::Object> target) {
    NanScope();

    Local<FunctionTemplate> tpl = NanNew<FunctionTemplate>(JSNewFunction);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(NanNew<String>("StatusList"));

          NODE_SET_PROTOTYPE_METHOD(tpl, "entrycount", Entrycount);
            NODE_SET_PROTOTYPE_METHOD(tpl, "free", Free);
            NODE_SET_PROTOTYPE_METHOD(tpl, "getPerfdata", GetPerfdata);
             NODE_SET_METHOD(tpl, "create", Create);
    
    Local<Function> _constructor_template = tpl->GetFunction();
    NanAssignPersistent(constructor_template, _constructor_template);
    target->Set(NanNew<String>("StatusList"), _constructor_template);
  }

  NAN_METHOD(GitStatusList::JSNewFunction) {
    NanScope();

    if (args.Length() == 0 || !args[0]->IsExternal()) {
         return NanThrowError("A new GitStatusList cannot be instantiated.");
     }

    GitStatusList* object = new GitStatusList(static_cast<git_status_list *>(Handle<External>::Cast(args[0])->Value()), args[1]->BooleanValue());
    object->Wrap(args.This());

    NanReturnValue(args.This());
  }

  Handle<v8::Value> GitStatusList::New(void *raw, bool selfFreeing) {
    NanEscapableScope();
    Handle<v8::Value> argv[2] = { NanNew<External>((void *)raw), NanNew<Boolean>(selfFreeing) };
    return NanEscapeScope(NanNew<Function>(GitStatusList::constructor_template)->NewInstance(2, argv));
  }

  git_status_list *GitStatusList::GetValue() {
    return this->raw;
  }

  git_status_list **GitStatusList::GetRefValue() {
    return this->raw == NULL ? NULL : &this->raw;
  }

  void GitStatusList::ClearValue() {
    this->raw = NULL;
  }

  
/*
     * @return Number  result    */
NAN_METHOD(GitStatusList::Entrycount) {
  NanEscapableScope();

   size_t result = git_status_list_entrycount(
ObjectWrap::Unwrap<GitStatusList>(args.This())->GetValue()
  );

 
  Handle<v8::Value> to;
// start convert_to_v8 block
     to = NanNew<Number>( result);
  // end convert_to_v8 block
  NodeGitPsueodoNanReturnEscapingValue(to);
}
   
/*
     */
NAN_METHOD(GitStatusList::Free) {
  NanEscapableScope();

if (ObjectWrap::Unwrap<GitStatusList>(args.This())->GetValue() != NULL) {
 git_status_list_free(
ObjectWrap::Unwrap<GitStatusList>(args.This())->GetValue()
  );

  ObjectWrap::Unwrap<GitStatusList>(args.This())->ClearValue();
}
   NanReturnUndefined();
}
  
/*
     * @param DiffPerfdata callback
   */
NAN_METHOD(GitStatusList::GetPerfdata) {
  NanScope();

  if (args.Length() == 0 || !args[0]->IsFunction()) {
    return NanThrowError("Callback is required and must be a Function.");
  }

  GetPerfdataBaton* baton = new GetPerfdataBaton;

  baton->error_code = GIT_OK;
  baton->error = NULL;

  baton->status = ObjectWrap::Unwrap<GitStatusList>(args.This())->GetValue();

  NanCallback *callback = new NanCallback(Local<Function>::Cast(args[0]));
  GetPerfdataWorker *worker = new GetPerfdataWorker(baton, callback);
  worker->SaveToPersistent("status", args.This());

  NanAsyncQueueWorker(worker);
  NanReturnUndefined();
}

void GitStatusList::GetPerfdataWorker::Execute() {
  int result = git_status_list_get_perfdata(
baton->out,baton->status    );

  baton->error_code = result;

  if (result != GIT_OK && giterr_last() != NULL) {
    baton->error = git_error_dup(giterr_last());
  }

}

void GitStatusList::GetPerfdataWorker::HandleOKCallback() {
  TryCatch try_catch;

  if (baton->error_code == GIT_OK) {
    Handle<v8::Value> to;
// start convert_to_v8 block
  
  if (baton->out != NULL) {
    // GitDiffPerfdata baton->out
       to = GitDiffPerfdata::New((void *)baton->out, false);
   }
  else {
    to = NanNull();
  }

 // end convert_to_v8 block
    Handle<v8::Value> result = to;
    Handle<v8::Value> argv[2] = {
      NanNull(),
      result
    };
    callback->Call(2, argv);
  } else {
    if (baton->error) {
      Handle<v8::Value> argv[1] = {
        NanError(baton->error->message)
      };
      callback->Call(1, argv);
      if (baton->error->message)
        free((void *)baton->error->message);
      free((void *)baton->error);
    } else {
      callback->Call(0, NULL);
    }

  }

  if (try_catch.HasCaught()) {
    node::FatalException(try_catch);
  }


  delete baton;
}

   
/*
  * @param Repository repo
   * @param StatusOptions opts
     * @return StatusList out    */
NAN_METHOD(GitStatusList::Create) {
  NanEscapableScope();

  if (args.Length() == 0 || !args[0]->IsObject()) {
    return NanThrowError("Repository repo is required.");
  }

  git_status_list * out = 0;
// start convert_from_v8 block
  git_repository * from_repo;
  from_repo = ObjectWrap::Unwrap<GitRepository>(args[0]->ToObject())->GetValue();
// end convert_from_v8 block
// start convert_from_v8 block
  const git_status_options * from_opts;
  if (args[1]->IsObject()) {
  from_opts = ObjectWrap::Unwrap<GitStatusOptions>(args[1]->ToObject())->GetValue();
  }
  else {
    from_opts = 0;
  }
// end convert_from_v8 block
 git_status_list_new(
&out
,from_repo
,from_opts
  );

 
  Handle<v8::Value> to;
// start convert_to_v8 block
  
  if (out != NULL) {
    // GitStatusList out
       to = GitStatusList::New((void *)out, false);
   }
  else {
    to = NanNull();
  }

 // end convert_to_v8 block
  NodeGitPsueodoNanReturnEscapingValue(to);
}
      Persistent<Function> GitStatusList::constructor_template;
 