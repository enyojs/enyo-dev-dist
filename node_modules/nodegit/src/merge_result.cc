// This is a generated file, modify: generate/templates/class_content.cc.
#include <nan.h>
#include <string.h>
#include <chrono>
#include <thread>

extern "C" {
  #include <git2.h>
 }

#include "../include/functions/copy.h"
#include "../include/macros.h"
#include "../include/merge_result.h"

 
#include <iostream>

using namespace std;
using namespace v8;
using namespace node;

  GitMergeResult::GitMergeResult(git_merge_result *raw, bool selfFreeing) {
    this->raw = raw;
    this->selfFreeing = selfFreeing;
  }

  GitMergeResult::~GitMergeResult() {
 
    // this will cause an error if you have a non-self-freeing object that also needs
    // to save values. Since the object that will eventually free the object has no
    // way of knowing to free these values.
 
  }

  void GitMergeResult::InitializeComponent(Handle<v8::Object> target) {
    NanScope();

    Local<FunctionTemplate> tpl = NanNew<FunctionTemplate>(JSNewFunction);

    tpl->InstanceTemplate()->SetInternalFieldCount(1);
    tpl->SetClassName(NanNew<String>("MergeResult"));

  
    Local<Function> _constructor_template = tpl->GetFunction();
    NanAssignPersistent(constructor_template, _constructor_template);
    target->Set(NanNew<String>("MergeResult"), _constructor_template);
  }

  NAN_METHOD(GitMergeResult::JSNewFunction) {
    NanScope();

    if (args.Length() == 0 || !args[0]->IsExternal()) {
         return NanThrowError("A new GitMergeResult cannot be instantiated.");
     }

    GitMergeResult* object = new GitMergeResult(static_cast<git_merge_result *>(Handle<External>::Cast(args[0])->Value()), args[1]->BooleanValue());
    object->Wrap(args.This());

    NanReturnValue(args.This());
  }

  Handle<v8::Value> GitMergeResult::New(void *raw, bool selfFreeing) {
    NanEscapableScope();
    Handle<v8::Value> argv[2] = { NanNew<External>((void *)raw), NanNew<Boolean>(selfFreeing) };
    return NanEscapeScope(NanNew<Function>(GitMergeResult::constructor_template)->NewInstance(2, argv));
  }

  git_merge_result *GitMergeResult::GetValue() {
    return this->raw;
  }

  git_merge_result **GitMergeResult::GetRefValue() {
    return this->raw == NULL ? NULL : &this->raw;
  }

  void GitMergeResult::ClearValue() {
    this->raw = NULL;
  }

     Persistent<Function> GitMergeResult::constructor_template;
 